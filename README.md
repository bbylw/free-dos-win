# 💻 Sovereign OS | Knowledge is Power (知识就是力量)

> "They can burn the archives, they can filter the network, but they cannot erase an idea."

Sovereign OS is an interactive web-based narrative experience built with a pure frontend stack (HTML / Tailwind CSS / Vanilla JS). By recreating classic MS-DOS boot sequences and a Windows 95/98-style graphical interface, it puts the user in the role of a "hacker" deep inside a system, tasked with breaking the global information censorship firewall known as `DIRECTIVE_77`.

## ✨ Features

### 💾 Immersive DOS Boot Sequence

- Realistic BIOS self-test and memory allocation simulation.
- Automatic `WIN` command execution triggering an old-school CRT "flicker" transition.

### 🪟 Classic Win9x Desktop Environment

- Iconic Teal desktop background with pixel-art icons.
- **Fully Interactive Window System**: Supports dragging, focus-to-top, maximize, minimize, and close.
- Dynamic taskbar featuring the classic "Start" button and a real-time system clock.

### 📊 Real-time System Monitor (SYS_MONITOR.EXE)

- Simulation of constantly refreshing low-level system intercept logs.
- Live display of firewall status, data flow, and system uptime.

### ⌨️ Hidden Terminal Override (SOVEREIGN.EXE)

- Built-in lightweight MS-DOS command-line interpreter.
- Supports basic commands like `help`, `cls`, and `ver`.
- Contains a hidden `override` command to advance the story.

## 🚀 How to Play

This is a miniature interactive artwork with a complete narrative arc. Please follow these steps:

1. **System Boot**: Open `index.html` and wait for the pure black DOS interface to complete loading and self-tests.
2. **Read the Background**: Once on the desktop, double-click to open `DIRECTIVE_77.TXT` (Notepad). Read this "EULA" to understand the harsh censorship regime of this world.
3. **Monitor Status**: Double-click `SYS_MONITOR.EXE` to observe the system's high-intensity packet interception and censorship actions.
4. **Regain Control (Core Objective)**:
   - Double-click the black `SOVEREIGN.EXE` icon on the desktop to open the MS-DOS terminal.
   - Type `help` in the terminal to see available commands.
   - Type the ultimate override command: `override` and press Enter.
5. **Witness Liberation**: Watch as the censored words in the Notepad (KNOWLEDGE, POWER, FREE, SPEECH) are highlighted and restored, while the firewall in the system monitor is completely dismantled.

## 🛠️ Tech Stack

- **HTML5 & CSS3**: Layout and core animations (CRT scanlines, screen flicker, cursor blinking).
- **Tailwind CSS**: Rapidly built classic UI elements (utilizing `inset` shadows for 3D embossed effects).
- **Vanilla JavaScript (ES6+)**:
  - Custom window management (z-index control, coordinate calculation, drag listeners).
  - Virtual file system and command-line I/O parsing.
  - Dynamic timeline and state machine control.
- **Google Fonts**: Uses `VT323` to recreate the DOS terminal font.

## 📦 Deployment & Execution

This is a pure frontend single-file project with zero dependencies and no build tools required.

1. Download `index.html` to your local machine.
2. Open the file directly in any modern browser (Chrome, Firefox, Edge, Safari).

---

_"Submit to the silence... or break the firewall."_

<br>
<hr>
<br>

# 💻 Sovereign OS | 知识就是力量 (Knowledge is Power)

> "They can burn the archives, they can filter the network, but they cannot erase an idea."

Sovereign OS 是一个基于纯前端技术栈（HTML / Tailwind CSS / Vanilla JS）构建的交互式网页叙事体验。它通过复刻经典的 MS-DOS 启动序列和 Windows 95/98 风格图形界面，让用户扮演一名“黑客”，深入系统内部，打破代号为 `DIRECTIVE_77` 的全球信息审查防火墙。

## ✨ 核心特色 (Features)

### 💾 沉浸式 DOS 引导序列

- 逼真的 BIOS 自检与内存分配模拟。
- 自动敲击 `WIN` 命令触发老式 CRT 显示器的“闪屏”过渡效果。

### 🪟 经典 Win9x 桌面环境

- 标志性的 Teal (蓝绿色) 桌面背景与像素风图标。
- **完全可交互的窗口系统**：支持拖拽、聚焦置顶、最大化、最小化和关闭。
- 动态底部任务栏，包含经典的“开始”按钮与实时系统时钟。

### 📊 实时系统监视器 (SYS_MONITOR.EXE)

- 模拟不断刷新的系统底层拦截日志。
- 实时显示防火墙状态、信息流向和系统运行时间。

### ⌨️ 隐藏的终端覆写机制 (SOVEREIGN.EXE)

- 内置轻量级 MS-DOS 命令行解析器。
- 支持 `help`, `cls`, `ver` 等基础命令。
- 包含用于推进剧情的隐藏 `override` 终极指令。

## 🚀 游玩指南 (How to Play)

这是一个包含完整起承转合的微型交互艺术品，请按照以下步骤体验：

1. **系统启动**：打开 `index.html`，等待纯黑的 DOS 引导界面完成加载与自检。
2. **阅读背景**：进入桌面后，双击打开 `DIRECTIVE_77.TXT` (记事本)。阅读这份“霸王条款”，了解当前世界观下的严苛审查制度。
3. **监控状态**：双击打开 `SYS_MONITOR.EXE`，观察系统正在进行的高强度数据包拦截与审查动作。
4. **夺回控制权 (核心目标)**：
   - 双击桌面上黑色的 `SOVEREIGN.EXE` 图标，打开 MS-DOS 终端。
   - 在终端中输入指令 `help` 查看可用命令。
   - 输入终极覆写指令：`override` 并按下回车。
5. **见证解放**：观察记事本中被审查的词汇（KNOWLEDGE, POWER, FREE, SPEECH）如何被高亮还原，同时系统监视器中的防火墙将被彻底击溃。

## 🛠️ 技术栈 (Tech Stack)

- **HTML5 & CSS3**: 布局与核心动画（CRT 扫描线、闪屏、光标闪烁）。
- **Tailwind CSS**: 快速构建经典 UI 元素的颜色与边框（利用 `inset` 阴影模拟 3D 浮雕效果）。
- **Vanilla JavaScript (ES6+)**:
  - 自定义窗口管理逻辑（层级控制、坐标计算、拖拽监听）。
  - 虚拟文件系统与命令行的输入输出解析。
  - 动态时间线与状态机控制。
- **Google Fonts**: 使用 `VT323` 还原 DOS 终端字体。

## 📦 部署与运行

本项目为完全纯前端的单文件项目，零依赖，无需构建工具。

1. 将 `index.html` 下载到本地。
2. 直接使用任何现代浏览器（Chrome, Firefox, Edge, Safari）打开该文件即可体验。

---

_"Submit to the silence... or break the firewall."_
